import { FileNameDirective } from './file-name.directive';

describe('FileNameDirective', () => {
  it('should create an instance', () => {
    const directive = new FileNameDirective();
    expect(directive).toBeTruthy();
  });
});
