import { NotebookNameDirective } from './notebook-name.directive';

describe('NotebookNameDirective', () => {
  it('should create an instance', () => {
    const directive = new NotebookNameDirective();
    expect(directive).toBeTruthy();
  });
});
