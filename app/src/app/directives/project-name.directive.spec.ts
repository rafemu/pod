import { ProjectNameDirective } from './project-name.directive';

describe('ProjectNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectNameDirective();
    expect(directive).toBeTruthy();
  });
});
