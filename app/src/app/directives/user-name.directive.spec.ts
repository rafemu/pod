import { UserNameDirective } from './user-name.directive';

describe('UserNameDirective', () => {
  it('should create an instance', () => {
    const directive = new UserNameDirective();
    expect(directive).toBeTruthy();
  });
});
